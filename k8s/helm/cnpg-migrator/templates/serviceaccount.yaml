{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cnpg-migrator.serviceAccountName" . }}
  namespace: {{ include "cnpg-migrator.namespace" . }}
  labels:
    {{- include "cnpg-migrator.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
